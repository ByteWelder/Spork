branches:
  only:
    - master
    - 3.2.0

language: java
sudo: false

jdk:
  - oraclejdk8
 
script:
  - ./gradlew check --continue --info

cache:
  directories:
    - $HOME/.gradle
    - $HOME/.m2/repository

after_success:
- ./gradlew test