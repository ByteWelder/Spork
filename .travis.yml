language: android
jdk: oraclejdk8

android:
  components:
    - build-tools-23.0.1
    - android-24

script:
  - ./gradlew spork:check spork-mockito:check --continue --info

cache:
  directories:
    - $HOME/.gradle
    - $HOME/.m2/repository

after_success:
  - echo success